<div class="page_title">
    <h1>タスクの変更</h1>
</div>

<div class="form_area">
    <form action="/edittask/<%= locals.companyId %>/<%= locals.taskId %>?from=<%= locals.from %>&_method=PUT" method="POST">
        <label for="taskTitle">タスク内容</label>
        <input type="text" name="taskTitle" placeholder="タスクの内容" value="<%= locals.taskTitle %>" required><br>
        <label for="dueDate">期日・締め切り</label>
        <input type="datetime-local" name="dueDate" value="<%= locals.dueDate %>"><br>
        <label for="note">備考・メモ</label>
        <textarea name="note" placeholder="メモ" rows="5" cols="25" value="<%= locals.note %>"></textarea><br>
        <label for="orderNum">優先度・順番</label><br>
        <select name="orderNum">
            <option>1</option>
            <option>2</option>
            <option>3</option>
            <option>4</option>
            <option>5</option>
            <option>6</option>
            <option>7</option>
            <option>8</option>
            <option>9</option>
            <option>10</option>
        </select><br>
        <button type="submit">タスクを変更</button>
    </form>
</div>
<div class="button_area">
    <a href="/progress/<%= locals.companyId %>"><button>前へ戻る</button></a>
</div>
