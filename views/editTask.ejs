<div class="page_title">
    <h1>タスクの変更</h1>
</div>

<div class="form_area">
    <form action="/edittask/<%= locals.companyId %>/<%= locals.taskId %>?from=<%= locals.from %>&_method=PUT" method="POST">
        <input type="text" name="taskTitle" placeholder="タスクの内容" value="<%= locals.taskTitle %>" required>
        <input type="datetime-local" name="dueDate" value="<%= locals.dueDate %>">
        <textarea name="note" placeholder="メモ" rows="5" value="<%= locals.note %>"></textarea>
        <label for="orderNum">優先度・順番</label>
        <select name="orderNum">
            <option>1</option>
            <option>2</option>
            <option>3</option>
            <option>4</option>
            <option>5</option>
            <option>6</option>
            <option>7</option>
            <option>8</option>
            <option>9</option>
            <option>10</option>
        </select>
        <button type="submit">タスクを変更</button>
    </form>
</div>
<div class="button_area">
    <a href="/progress/<%= locals.companyId %>"><button>前へ戻る</button></a>
</div>
